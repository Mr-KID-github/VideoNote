# ============ 服务配置 ============
HOST=0.0.0.0
PORT=8900

# ============ LLM 配置 ============
# 支持所有 OpenAI 兼容 API（OpenAI / DeepSeek / 通义千问 / Ollama 等）
LLM_API_KEY=sk-your-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# ============ 转写配置 ============
# 转写器类型: groq (推荐，云端零依赖) / whisper (本地，需装 openai-whisper)
TRANSCRIBER_TYPE=groq

# Groq Whisper API (推荐，https://console.groq.com/keys 获取免费 Key)
GROQ_API_KEY=gsk_your-groq-api-key

# 本地 Whisper (仅 TRANSCRIBER_TYPE=whisper 时需要)
# WHISPER_MODEL_SIZE=base
# WHISPER_DEVICE=cpu

# ============ 存储路径 ============
DATA_DIR=data
OUTPUT_DIR=output
