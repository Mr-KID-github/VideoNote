# Frontend runtime config
# In dev you can leave this empty to use same-origin /api through the app proxy.
VITE_API_BASE_URL=
# Optional standalone docs site URL. When empty, the app falls back to backend Swagger docs.
VITE_DOCS_BASE_URL=
